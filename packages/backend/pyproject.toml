[project]
name = "findog-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.35.0",
  "pydantic>=2.11.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
