# UI Design Notes

- Storybook stories are the source of truth for component design and behavior.
- Primary user journey: Login -> Save product -> View price trends -> Set restock alerts.
- All UI components must go through the design system (`packages/frontend/design-system`).
- Screen mockups and wireframes should be stored in `docs/ui/` as images or linked Figma URLs.
